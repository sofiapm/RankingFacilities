<h1 class="page-header"> Facility <%= @facility.name %> Information</h1>

<% @action = {:action => 'edit'} %>

<%= render 'form', @action %>

<td><%= link_to 'New Facility Attribute', new_facility_facility_static_measure_path(params[:id]), class: "btn btn-default" %></td>
<td><%= link_to 'Facility Attributes', facility_facility_static_measures_path(params[:id]), class: "btn btn-default" %></td>